mod lexer;
#[cfg(test)]
mod tests;

fn main() {
    // You can now use the `token` enum as:
    // For example, to use a variant (replace `SomeVariant` with an actual variant name):
    // let _ = lexer::token::Token::SomeVariant;
}
